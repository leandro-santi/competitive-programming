#include<bits/stdc++.h>

using namespace std;

int main(){
    string s, aux;
    cin >> s;

    aux.append(s, 0, 3);

    aux == "555" ? (cout << "1") << endl : (cout << "0") << endl;
    
    return 0;
}